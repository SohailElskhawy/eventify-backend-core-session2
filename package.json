{
  "name": "eventify",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Eventify - the event booking API built across the course",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "dev": "node --watch src/domain.ts",
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "test": "echo \"No tests yet - Vitest arrives in Session 6\" && exit 0"
  },
  "devDependencies": {
    "@types/node": "^24.13.3",
    "eslint": "^9.30.0",
    "typescript": "^5.9.0",
    "typescript-eslint": "^8.40.0"
  }
}
